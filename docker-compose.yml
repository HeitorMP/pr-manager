version: '3.8'

services:
  pr-manager:
    build: .
    image: pr-manager-tui
    container_name: pr-manager
    environment:
      - GITHUB_TOKEN=${GITHUB_TOKEN}
      - GITHUB_ORG=${GITHUB_ORG}
    stdin_open: true
    tty: true
